class StringsAndFiles
{
    public static void main(String [] args)
    {
        /*
        //Declare
        stringy = new String("This is a string");
        // or s = new String("This is a string");

        //Print
        System.out.print(stringy);
        //or on a new line System.out.println(stringy);

        //Concatenation
        System.out.print(stringy + "and this is added on");

        //String Functions (a VERY brief overview)
        int stringyLength = stringy.length();
        //Gives the length of the string
        charInStringy = stringy.charAt(stringyLength); //Will return error because 0-n instead of 1-n
        //Selects a specific index in a string
        stringy.substring(x,y);
        //Returns a substring or a string from the string

        //To read
        cin.next(); //Assuming cin is a scanner object (any scanner will work)
        //Reads in a new string from a scanner
        cin.nextLine();
        //Reads up until the next line character

        */
    }
}